# ⚙️ 設定 Git 用戶資訊

## ❌ 錯誤訊息

您看到的錯誤：
```
fatal: unable to auto-detect email address
Author identity unknown
*** Please tell me who you are.
```

**原因：** Git 需要知道您的身份才能建立提交（commit）。

---

## ✅ 解決方法（2 個步驟）

### 步驟 1：設定您的姓名

在終端機執行（**將「您的姓名」替換成您的真實姓名**）：

```bash
git config --global user.name "您的姓名"
```

**例如：**
```bash
git config --global user.name "Tina Chen"
```

或使用中文：
```bash
git config --global user.name "陳小華"
```

---

### 步驟 2：設定您的電子郵件

在終端機執行（**使用您的 GitHub 電子郵件**）：

```bash
git config --global user.email "您的電子郵件"
```

**例如：**
```bash
git config --global user.email "tina@example.com"
```

**重要：** 建議使用您 GitHub 帳號的電子郵件！

---

## 🎯 快速設定（複製貼上）

**請將以下命令中的資訊替換成您的真實資訊，然後執行：**

```bash
git config --global user.name "您的姓名"
git config --global user.email "您的電子郵件"
```

**例如：**
```bash
git config --global user.name "Tina"
git config --global user.email "0330tina@gmail.com"
```

---

## 📧 如何找到您的 GitHub 電子郵件？

### 方法 1：查看 GitHub 設定

1. 前往 GitHub → 右上角頭像 → **Settings**
2. 左側選單 → **Emails**
3. 您會看到所有已驗證的電子郵件
4. 複製其中一個使用

### 方法 2：使用 GitHub 的 no-reply 電子郵件

GitHub 提供一個專用的 no-reply 電子郵件格式：
```
您的用戶名@users.noreply.github.com
```

**例如，如果您的用戶名是 `0330tina`：**
```bash
git config --global user.email "0330tina@users.noreply.github.com"
```

**優點：** 可以保護您的真實電子郵件不被公開

---

## ✅ 驗證設定

設定完成後，執行以下命令確認：

```bash
git config --global user.name
git config --global user.email
```

應該會顯示您剛才設定的姓名和電子郵件。

---

## 🚀 完成後繼續提交

設定完成後，再次執行：

```bash
git commit -m "Initial commit: IHCA risk prediction system"
```

這次應該會成功！

---

## 💡 關於 `--global` 參數

- **`--global`**：設定會套用到這台電腦的所有 Git 倉庫（推薦）
- **不加 `--global`**：只套用到當前這個專案

**建議使用 `--global`**，這樣以後就不需要再設定了。

---

## 📋 完整流程

```bash
# 1. 設定姓名
git config --global user.name "您的姓名"

# 2. 設定電子郵件
git config --global user.email "您的電子郵件"

# 3. 驗證設定
git config --global user.name
git config --global user.email

# 4. 提交
git commit -m "Initial commit: IHCA risk prediction system"

# 5. 推送
git push -u origin main
```

---

## 🆘 常見問題

### Q: 可以使用假名嗎？
**A:** 可以，但建議使用真實姓名或 GitHub 用戶名，這樣在 GitHub 上會顯示正確的作者資訊。

### Q: 電子郵件必須是 GitHub 的嗎？
**A:** 不一定，但建議使用 GitHub 帳號的電子郵件，這樣您的提交會正確連結到您的 GitHub 帳號。

### Q: 設定後可以修改嗎？
**A:** 可以！隨時執行相同的命令就可以修改。

---

## ✨ 完成！

設定完成後，您就可以正常使用 Git 了！


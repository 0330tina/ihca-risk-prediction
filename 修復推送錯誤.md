# 🔧 修復 Git 推送錯誤：src refspec main does not match any

## ❌ 錯誤原因

您看到的錯誤：
```
error: src refspec main does not match any
error: failed to push some refs to 'https://github.com/0330tina/ihca-risk-prediction.git'
```

**原因：** 您還沒有執行 `git commit`，所以本地沒有 `main` 分支可以推送！

---

## ✅ 解決步驟

### 步驟 1：檢查狀態

在終端機執行：

```bash
git status
```

**您應該會看到類似：**
```
Changes to be committed:
  (use "git reset HEAD <file>..." to unstage)
        new file:   ...
```

這表示檔案已經添加到暫存區，但還沒有提交。

---

### 步驟 2：執行提交（最重要！）

```bash
git commit -m "Initial commit: IHCA risk prediction system"
```

**預期結果：**
```
[main (root-commit) xxxxxxx] Initial commit: IHCA risk prediction system
 XX files changed, XXXX insertions(+)
```

---

### 步驟 3：確認分支

```bash
git branch
```

**應該會顯示：**
```
* main
```

這表示您現在在 `main` 分支上。

---

### 步驟 4：再次推送

```bash
git push -u origin main
```

這次應該會成功！

---

## 📋 完整流程（從頭開始）

如果您想確認所有步驟都正確，可以按順序執行：

```bash
# 1. 檢查狀態
git status

# 2. 確認檔案已添加（如果沒有，執行 git add .）
git add .

# 3. 提交（這一步您可能漏掉了！）
git commit -m "Initial commit: IHCA risk prediction system"

# 4. 確認分支
git branch

# 5. 推送
git push -u origin main
```

---

## 🎯 快速修復（複製貼上）

直接複製以下命令，一行一行執行：

```bash
git commit -m "Initial commit: IHCA risk prediction system"
git push -u origin main
```

---

## 💡 為什麼會發生這個錯誤？

Git 的工作流程是：
1. ✅ `git init` - 初始化倉庫（已完成）
2. ✅ `git add .` - 添加檔案到暫存區（已完成）
3. ❌ `git commit` - **提交變更（這一步您可能漏掉了！）**
4. ❌ `git push` - 推送到遠端（失敗，因為沒有提交）

**沒有提交 = 沒有分支 = 無法推送**

---

## ✅ 完成後

推送成功後，您會看到：

```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
Writing objects: 100% (XX/XX), done.
To https://github.com/0330tina/ihca-risk-prediction.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

然後就可以在 GitHub 上看到您的專案了！

---

## 🆘 如果還是有問題

執行以下命令檢查：

```bash
# 查看所有分支
git branch -a

# 查看提交歷史
git log

# 查看遠端設定
git remote -v
```

告訴我這些命令的輸出，我可以進一步協助！

